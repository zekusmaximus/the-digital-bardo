{
  "name": "the-digital-bardo",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest",
    "deploy": "npm run build && netlify deploy",
    "deploy:prod": "npm run build && netlify deploy --prod"
  },
  "devDependencies": {
    "jsdom": "^26.1.0",
    "netlify-cli": "^17.23.1",
    "vite": "^5.2.12",
    "vite-plugin-singlefile": "^2.3.0",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "gsap": "^3.13.0",
    "three": "^0.165.0"
  }
}
